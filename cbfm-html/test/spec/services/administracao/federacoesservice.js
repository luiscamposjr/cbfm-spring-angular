'use strict';

describe('Service: administracao/federacoesService', function () {

  // load the service's module
  beforeEach(module('cbfmHtmlApp'));

  // instantiate service
  var administracao/federacoesService;
  beforeEach(inject(function (_administracao/federacoesService_) {
    administracao/federacoesService = _administracao/federacoesService_;
  }));

  it('should do something', function () {
    expect(!!administracao/federacoesService).toBe(true);
  });

});
